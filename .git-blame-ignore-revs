# Commits that touched a lot of lines without changing what the code does.
# `git blame` skips them and reports the last real author of each line instead.
#
# GitHub reads this file automatically in its blame view. For local git, turn
# it on once per clone:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is genuinely content-free — a reformat, a
# renormalise, a rename. Verify before adding, e.g.:
#
#   git show <sha> --format= --ignore-cr-at-eol --numstat
#
# One entry per line: the full 40-character SHA, with the subject as a comment
# above it.

# Normalise line endings to LF (.gitattributes + `git add --renormalize .`)
7718cd75bdf992ecb9b45cde2a5b0b08d1c76ab7
